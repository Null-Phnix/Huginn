# HUGINN v1.1-beta Implementation Plan

> **Goal:** Add killer features — enhanced Actions API, smart rendering, and structured extraction with Pydantic schema.
> **Baseline:** 189 tests passing (v1.1-alpha complete)
> **Test Command:** `cd /mnt/GameDrive/AI_Projects/BlackCrawl && python -m pytest tests/ -v --tb=short`
> **Architecture:** Incremental improvements. No breaking changes. All new features are additive.

---

## Task 6: Enhanced Actions API

**Current state:** `execute_actions` supports click, wait, scroll, screenshot, type, press. Missing: `select` (dropdown), `hover`, `wait_for_selector`.

### Steps:
1. Add `SELECT`, `HOVER`, `WAIT_FOR_SELECTOR` to `ActionType` enum in `models.py`
2. Add optional `values` field to `Action` model (for multi-select)
3. Implement all three in `execute_actions` in `browser.py`
4. Write tests in `tests/test_browser.py`

---

## Task 7: Smart Rendering (JS detection + lightweight path)

**Current state:** Always uses full Playwright rendering, even for static pages.

### Steps:
1. Add `RenderMode` enum: `AUTO`, `FULL`, `LIGHT` in `browser.py`
2. Add `detect_render_mode()` function that checks response headers for JS indicators
3. Add `render_mode` field to `ScrapeRequest` and `ScrapeOptions`
4. Implement lightweight rendering path using httpx + html→markdown conversion
5. Wire render_mode through scraper and API
6. Write tests in `tests/test_scraper.py`

---

## Task 8: Structured Extraction Enhancement

**Current state:** `/v1/distill` has prompt-based extraction with schema validation and mental model retry.

### Steps:
1. Add `format` field to `ExtractOptions`: `"text" | "markdown" | "json"`
2. Add `format` to `DistillRequest` (the `/v1/distill` request model) — wait, it's called `ExtractRequest`
3. When format is "json" and schema provided, enforce strict validation
4. Return appropriate content type based on format
5. Write tests in `tests/test_extractor.py`

---

## Implementation Order
1. Task 6 (Actions API) — smallest, self-contained
2. Task 7 (Smart Rendering) — moderate, adds bs4 dependency
3. Task 8 (Structured Extraction) — touches existing extractor code
